{
  "name": "bank-statement-db-api",
  "version": "1.0.0",
  "description": "Minimal API that saves Date + Description rows from the Bank Statement to Excel tool into a Railway PostgreSQL database.",
  "main": "server.js",
  "scripts": {
    "start": "node server.js"
  },
  "dependencies": {
    "express": "^4.19.2",
    "cors": "^2.8.5",
    "pg": "^8.12.0"
  },
  "engines": {
    "node": ">=18"
  }
}
